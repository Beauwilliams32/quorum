"use client";

import { useEffect, useMemo, useState } from "react";

type Status = "Active" | "Ready" | "Blocked" | "External input";
type Project = { name:string; area:string; status:Status; progress:number; verification:string; next:string; blocker:string; accent:string };
type Runtime = { id:string; label:string; kind:string; available:boolean; authReady:boolean; capabilities:string[]; command?:string };
type Catalog = { runtimes:Runtime[]; models:{id:string;provider:string;kind:string;available:boolean;harnessId:string}[]; config:{projectCount:number;runtimeCount:number;modelCount:number}; pets:{subjectId:string;source:string}[] };
type Operations = { overview:{activeSessions:number;sessions:number;agents:number;trackedPtys:number;skills:number;nodes:number}; sessions:{id:string;agent:string;model:string|null;projectId:string|null;kind:string;active:boolean;summary:string}[]; agents:{sessionId:string;name:string;projectId:string|null;kind:string;status:string|null;chatCapable:boolean}[]; skills:{id:string;label:string;runtimes:number;ready:number}[]; nodes:{id:string;label:string;kind:string;status:string;detail:string}[]; channels:{id:string;label:string;status:string;detail:string}[]; cronJobs:{id:string;subject:string;status:string;live:boolean;projectId:string|null;agent:string|null}[]; events:{kind:string;ts:number;text:string}[] };

const projects: Project[] = [
  {name:"Northstar Build",area:"Product workspace",status:"Ready",progress:72,verification:"Checks and dry-run pass",next:"Provision the next integration",blocker:"Provider setup",accent:"violet"},
  {name:"Core Runtime",area:"Local system",status:"Active",progress:78,verification:"Tests and loopback health pass",next:"Finish installer validation",blocker:"Supported-platform proof",accent:"blue"},
  {name:"Service Layer",area:"Product workspace",status:"Active",progress:68,verification:"Frontend and service checks pass",next:"Run one safe authenticated journey",blocker:"Verified platform connections",accent:"cyan"},
  {name:"Web Surface",area:"Product workspace",status:"Active",progress:82,verification:"Deployment-safety checks pass",next:"Complete browser smoke testing",blocker:"External service readiness",accent:"orange"},
  {name:"Pilot Workspace",area:"Product workspace",status:"Blocked",progress:64,verification:"Dry-run and validation pass",next:"Run a role-separated beta",blocker:"Pilot access and compliance review",accent:"pink"},
  {name:"Automation Flow",area:"Product workspace",status:"Active",progress:55,verification:"Readiness gate documented",next:"Verify confirmation and digest flow",blocker:"Sender and notification setup",accent:"green"},
  {name:"Media Pipeline",area:"Local system",status:"Blocked",progress:61,verification:"Package and preflight pass",next:"Record a disposable host test",blocker:"Human review and distribution",accent:"yellow"},
  {name:"Tool Registry",area:"Product workspace",status:"Active",progress:63,verification:"Static surface established",next:"Browser-test export flows",blocker:"Custom-domain verification",accent:"blue"},
  {name:"Launch Surface",area:"Product workspace",status:"Active",progress:57,verification:"Portable handoff integrated",next:"Validate claims and public links",blocker:"Final launch decision",accent:"cyan"},
  {name:"Local Workspace",area:"Local system",status:"Active",progress:59,verification:"Readiness wave integrated",next:"Audit storytelling and edit flow",blocker:"Approved content",accent:"orange"},
  {name:"Delivery Queue",area:"Client implementation",status:"External input",progress:43,verification:"Deployment path documented",next:"Apply approved copy and assets",blocker:"Client assets and review",accent:"green"},
  {name:"Route Migration",area:"Client implementation",status:"Blocked",progress:38,verification:"Phase one tested",next:"Verify ownership and redirects",blocker:"Domain-control review",accent:"red"},
  {name:"Data Transfer",area:"Client implementation",status:"Blocked",progress:41,verification:"Validator documented",next:"Collect schedules and migration plan",blocker:"Client data access",accent:"red"},
  {name:"Context Bridge",area:"Local system",status:"Active",progress:76,verification:"Tests and status checks pass",next:"Review observations explicitly",blocker:"Approved local source",accent:"violet"},
  {name:"Channel Layer",area:"Local system",status:"Blocked",progress:49,verification:"Readiness audit pass",next:"Create a rights-safe remediation plan",blocker:"Original or permissioned content",accent:"red"},
  {name:"Simulation Runner",area:"Local system",status:"Active",progress:71,verification:"Paper-mode checks pass",next:"Maintain observability",blocker:"Live actions out of scope",accent:"yellow"},
  {name:"Extension Fleet",area:"Internal beta",status:"Blocked",progress:58,verification:"Bundles and tests pass",next:"Run human-controlled acceptance",blocker:"Host evidence and distribution",accent:"pink"},
];

const statusMeta: Record<Status,{label:string;className:string}> = {Active:{label:"Active",className:"active"},Ready:{label:"Ready to provision",className:"ready"},Blocked:{label:"Blocked",className:"blocked"},"External input":{label:"Needs input",className:"external"}};
const modelLibrary = [
  {name:"Claude",kind:"Cloud harness",source:"Anthropic CLI",pet:"vex",state:"Quorum-ready",detail:"Roundtable debate + local PTY"},
  {name:"Codex",kind:"Cloud harness",source:"OpenAI CLI",pet:"bolt",state:"Quorum-ready",detail:"Local project sessions"},
  {name:"Hermes",kind:"Local harness",source:"Loopback gateway",pet:"nib",state:"Detected",detail:"Background jobs + gateway health"},
  {name:"OpenClaw",kind:"Local harness",source:"Configurable runtime",pet:"sable",state:"Configurable",detail:"Add as a validated runtime"},
  {name:"Gemini",kind:"Cloud harness",source:"Configurable runtime",pet:"muse",state:"Configurable",detail:"BYO auth; never shown in UI"},
  {name:"ComfyUI / Wan",kind:"Local model engine",source:"Machine service",pet:"ledger",state:"Detected",detail:"Local media generation + pressure"},
];

export default function Home() {
  const [filter,setFilter] = useState<"All"|Status>("All");
  const [view,setView] = useState<"portfolio"|"operator">("portfolio");
  const [query,setQuery] = useState("");
  const [selected,setSelected] = useState<Project|null>(null);
  const [runtime,setRuntime] = useState({available:false, active:"—", rooms:"—", observed:"—"});
  const [catalog,setCatalog] = useState<Catalog|null>(null);
  const [operations,setOperations] = useState<Operations|null>(null);
  const [operatorTab,setOperatorTab] = useState<"overview"|"sessions"|"skills"|"nodes"|"events">("overview");
  const [roomId,setRoomId] = useState("");
  const [notice,setNotice] = useState("");
  useEffect(() => { let alive = true; const sync = () => fetch("/api/quorum").then((response) => response.json()).then((data) => { if (!alive || !data.available) return; const health = data.health ?? {}; const state = data.state ?? {}; const sessions = health.sessions ?? state.sessions ?? {}; const projectsState = health.projects ?? state.projects ?? state.projectRooms ?? {}; const rooms = state.projects?.rooms ?? []; setRoomId(String(rooms[0]?.id ?? "")); setRuntime({available:true, active:String(sessions.active ?? state.activeSessions ?? "live"), rooms:String(Array.isArray(projectsState) ? projectsState.length : projectsState.total ?? state.projectCount ?? "live"), observed:String(sessions.total ?? state.sessionCount ?? "live")}); if (data.catalog) setCatalog(data.catalog); if (data.operations) setOperations(data.operations); }).catch(() => undefined); sync(); const timer = window.setInterval(sync, 3000); return () => { alive = false; window.clearInterval(timer); }; }, []);
  async function guardedLaunch(runtimeId:string) {
    if (!roomId) { setNotice("No Quorum project room is available for a guarded launch."); return; }
    setNotice(`Preparing a launch preview for ${runtimeId}…`);
    try { const response = await fetch("/api/quorum", { method:"POST", headers:{"content-type":"application/json"}, body:JSON.stringify({ action:"launch", runtimeId, roomId }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "preview failed"); if (!window.confirm(`${data.preview?.summary || "Launch runtime"}?`)) { setNotice("Launch cancelled."); return; } const confirmed = await fetch("/api/quorum", { method:"POST", headers:{"content-type":"application/json"}, body:JSON.stringify({ action:"launch", runtimeId, roomId, confirm:true }) }); const result = await confirmed.json(); setNotice(result.ok ? "Launch confirmed and recorded locally." : (result.error || "Launch was not executed.")); } catch (error) { setNotice(error instanceof Error ? error.message : "Quorum is offline; no launch was attempted."); }
  }
  const visible = useMemo(() => projects.filter((p) => { const haystack = `${p.name} ${p.area} ${p.next} ${p.blocker}`.toLowerCase(); return (filter === "All" || p.status === filter) && haystack.includes(query.toLowerCase()); }), [filter,query]);
  const blocked = projects.filter(p=>p.status === "Blocked").length;
  const active = projects.filter(p=>p.status === "Active" || p.status === "Ready").length;
  const avg = Math.round(projects.reduce((sum,p)=>sum+p.progress,0)/projects.length);

  return <main className="shell">
    <aside className="sidebar"><div className="brand"><span className="brand-mark">✦</span><span>TRIDENT<br/><b>COMMAND</b></span></div><div className="side-label">Workspace</div><nav>
      <button className={`nav-item ${view === "portfolio" ? "selected" : ""}`} onClick={()=>setView("portfolio")}><span>◈</span> Portfolio overview</button>
      <button className={`nav-item ${view === "operator" ? "selected" : ""}`} onClick={()=>setView("operator")}><span>⌬</span> Operator cockpit</button>
      <button className="nav-item" onClick={()=>{setView("portfolio");setFilter("Blocked")}}><span>⊘</span> Needs attention <em>{blocked}</em></button>
      <button className="nav-item" onClick={()=>{setView("portfolio");setFilter("Active")}}><span>↗</span> In motion <em>{active}</em></button>
    </nav><div className="side-label">View</div><nav>
      <button className="nav-item" onClick={()=>setFilter("All")}><span>▦</span> All projects</button>
      <button className="nav-item" onClick={()=>setFilter("External input")}><span>◎</span> Client delivery</button>
      <button className="nav-item" onClick={()=>setFilter("Ready")}><span>◆</span> Ready next</button>
    </nav><div className="sidebar-foot"><span className="pulse-dot"/> Source synced<br/><small>Portfolio map · Aug 20, 2026</small></div></aside>
    <section className="content"><header className="topbar"><div className="crumb">STUDIO OS <span>/</span> PORTFOLIO</div><div className="top-actions"><span className="updated">Last reviewed · Aug 20, 2026</span><button className="avatar" aria-label="Account">BW</button></div></header>
      <div className="intro"><div><p className="eyebrow">Good morning, operator <span>✦</span></p><h1>{view === "operator" ? "Run the room." : "Everything in motion."}</h1><p className="lede">{view === "operator" ? "Quorum convenes the argument. The local operator watches runtimes, sessions, tools, and handoffs in one operational picture." : "A live view of the work—what is moving, what is verified, and where attention unlocks the next step."}</p></div><button className="primary-btn" onClick={()=>view === "operator" ? setView("portfolio") : setFilter("All")}><span>{view === "operator" ? "↩" : "＋"}</span> {view === "operator" ? "Back to portfolio" : "Add project"}</button></div>
      <div className="metric-grid"><div className="metric-card hero-metric"><div className="metric-top"><span>Portfolio pulse</span><span className="metric-icon">↗</span></div><strong>{avg}%</strong><div className="metric-caption"><span className="trend">+8.4%</span> average readiness across {projects.length} projects</div><div className="sparkline">{Array.from({length:12},(_,i)=><i key={i}/>)}</div></div><div className="metric-card"><div className="metric-top"><span>In motion</span><span className="metric-icon green">◉</span></div><strong>{active}<small> / {projects.length}</small></strong><div className="metric-caption">Active or ready for the next gate</div><div className="mini-bar"><span style={{width:`${active/projects.length*100}%`}}/></div></div><div className="metric-card"><div className="metric-top"><span>Needs attention</span><span className="metric-icon red">!</span></div><strong>{blocked}<small> projects</small></strong><div className="metric-caption">Blocked by an external gate or risk</div><div className="attention-line"><span/> {projects.filter(p=>p.status === "Blocked").slice(0,3).map(p=>p.name.split(" ")[0]).join(" · ")}</div></div></div>
      {view === "portfolio" && <section className="project-landscape"><div className="section-head"><div><h2>Project landscape</h2><p>Readiness, verification, next move, and the gate holding each project.</p></div><div className="controls"><label className="search">⌕<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search projects"/></label><select value={filter} onChange={e=>setFilter(e.target.value as "All"|Status)}><option>All</option><option>Active</option><option>Ready</option><option>Blocked</option><option>External input</option></select></div></div><div className="project-list">{visible.map((project,index)=><button className="project-row" key={project.name} onClick={()=>setSelected(project)}><span className={`project-number ${project.accent}`}>{String(index+1).padStart(2,"0")}</span><span><span className="project-title">{project.name} <span className={`status ${statusMeta[project.status].className}`}><i/>{statusMeta[project.status].label}</span></span><span className="project-area">{project.area}</span></span><span className="project-next"><small>NEXT MOVE</small>{project.next}</span><span><span className="progress-label">{project.progress}%</span><span className="progress-track"><span className={`progress-fill ${project.accent}`} style={{width:`${project.progress}%`}}/></span></span><span className="row-arrow">→</span></button>)}</div></section>}
      {view === "operator" && <section className="operations"><div className="operations-head"><div><h2>Operator control plane</h2><p>OpenClaw-depth operations, backed by Quorum’s local collectors and guarded actions.</p></div><span className="safe-note">{operations ? `Live · ${new Date(operations.ts).toLocaleTimeString()}` : "Offline fallback"}</span></div><div className="ops-tabs">{([['overview','Overview'],['sessions','Sessions'],['skills','Skills'],['nodes','Nodes'],['events','Logs & events']] as const).map(([id,label])=><button key={id} className={operatorTab===id?'active':''} onClick={()=>setOperatorTab(id)}>{label}</button>)}</div>{operatorTab === "overview" && <><div className="ops-metrics">{[['Active sessions',operations?.overview.activeSessions ?? 0],['Tracked agents',operations?.overview.agents ?? 0],['Managed PTYs',operations?.overview.trackedPtys ?? 0],['Ready nodes',operations?.overview.nodes ?? 0]].map(([label,value])=><div className="ops-metric" key={String(label)}><small>{label}</small><strong>{value}</strong><span>local state</span></div>)}</div><div className="ops-columns"><div className="ops-panel"><div className="panel-title"><h3>Live sessions</h3><button onClick={()=>setOperatorTab('sessions')}>View all →</button></div>{(operations?.sessions ?? []).slice(0,5).map(session=><div className="session-row" key={session.id}><span className={`session-dot ${session.active?'live':''}`}/><div><b>{session.agent || 'agent'} <em>{session.model || session.kind}</em></b><p>{session.summary || 'Waiting for activity'}</p></div><small>{session.projectId || 'unassigned'}</small></div>)}{!operations?.sessions?.length && <div className="empty">No recent local sessions detected.</div>}</div><div className="ops-panel"><div className="panel-title"><h3>Gateway channels</h3><button onClick={()=>setOperatorTab('nodes')}>Inspect →</button></div>{(operations?.channels ?? []).map(channel=><div className="health-row" key={channel.id}><span className={`health-pill ${channel.status}`}>{channel.status}</span><div><b>{channel.label}</b><p>{channel.detail}</p></div></div>)}</div></div></>}{operatorTab === "sessions" && <div className="ops-panel wide"><div className="panel-title"><h3>Sessions and agents</h3><span>{operations?.sessions.length ?? 0} observed</span></div>{(operations?.sessions ?? []).map(session=><div className="session-row detailed" key={session.id}><span className={`session-dot ${session.active?'live':''}`}/><div><b>{session.agent || 'agent'} <em>{session.model || session.kind}</em></b><p>{session.summary || 'Waiting for activity'}</p></div><small>{session.projectId || 'unassigned'} · {session.active?'active':'idle'}</small></div>)}{(operations?.agents ?? []).map(agent=><div className="session-row detailed" key={agent.sessionId}><span className="pet-mini">✦</span><div><b>{agent.name} <em>{agent.status || agent.kind}</em></b><p>{agent.chatCapable ? 'Chat-capable agent' : 'Local agent presence'}</p></div><small>{agent.projectId || 'unassigned'}</small></div>)}{!operations?.sessions?.length && !operations?.agents?.length && <div className="empty">Start a catalogued runtime or connect OpenClaw to populate sessions.</div>}</div>}{operatorTab === "skills" && <div className="ops-panel wide"><div className="panel-title"><h3>Skills and capabilities</h3><span>Derived from safe runtime metadata</span></div><div className="skill-grid">{(operations?.skills ?? []).map(skill=><div className="skill-card" key={skill.id}><b>{skill.label}</b><p>{skill.ready} ready of {skill.runtimes} runtimes</p><div className="skill-bar"><span style={{width:`${skill.runtimes ? skill.ready/skill.runtimes*100 : 0}%`}}/></div></div>)}</div></div>}{operatorTab === "nodes" && <div className="ops-columns"><div className="ops-panel"><div className="panel-title"><h3>Nodes</h3><span>loopback only</span></div>{(operations?.nodes ?? []).map(node=><div className="health-row" key={node.id}><span className={`health-pill ${node.status}`}>{node.status}</span><div><b>{node.label}</b><p>{node.kind} · {node.detail}</p></div></div>)}</div><div className="ops-panel"><div className="panel-title"><h3>Guarded actions</h3><span>human-confirmed</span></div><div className="guarded-list"><p>Preview before launch, stop, route, or chain.</p><p>Only tracked PTYs and known project rooms are actionable.</p><p>Every confirmed action enters the local event feed.</p></div></div></div>}{operatorTab === "events" && <div className="ops-panel wide"><div className="panel-title"><h3>Logs and event feed</h3><span>redacted local activity</span></div>{(operations?.events ?? []).slice().reverse().map((event,index)=><div className="event-row" key={`${event.ts}-${index}`}><span>{event.kind}</span><p>{event.text}</p><small>{new Date(event.ts).toLocaleTimeString()}</small></div>)}{!operations?.events?.length && <div className="empty">No local events yet.</div>}</div>}<div className="model-library compact"><div className="library-head"><div><h2>Model + harness library</h2><p>Readiness metadata only. Provider credentials and private prompts stay local.</p></div><span className="safe-note">{catalog ? `${catalog.runtimes.length} runtimes · ${catalog.models.length} models` : "offline fallback"}</span></div><div className="model-grid">{(catalog?.runtimes ?? modelLibrary.map(model => ({id:model.name.toLowerCase().replaceAll(" ","-"),label:model.name,kind:model.kind,available:model.state === "Detected",authReady:false,capabilities:[],command:model.name.toLowerCase()}))).map((model)=><div className="model-card" key={model.id}><div className={`pet-avatar ${model.id}`} aria-label={`${model.label} pet avatar`}><span className="pet-ear left"/><span className="pet-ear right"/><span className="pet-face"><i/><i/></span><span className="pet-nose"/></div><div className="model-copy"><div className="model-name">{model.label}<span className={`model-state ${model.available ? "detected" : ""}`}>{model.available ? "Ready" : "Offline"}</span></div><div className="model-kind">{model.kind} harness · {model.capabilities?.slice(0,2).join(" · ") || "safe adapter"}</div><div className="model-detail">{model.authReady ? "Existing local auth detected" : "Readiness only · auth stays local"}</div></div><button className="outline-btn model-launch" disabled={!model.available || !model.command} onClick={()=>guardedLaunch(model.id)}>Preview launch</button></div>)}</div>{notice && <p className="operator-notice">{notice}</p>}</div></section>}
      <footer className="footer"><span>LOCAL COMMAND CENTER · OPEN CORE</span><span>Truthful status, clear next moves.</span></footer>
    </section>
    {selected&&<div className="modal-backdrop" onClick={()=>setSelected(null)}><article className="detail-modal" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setSelected(null)} aria-label="Close">×</button><span className={`status ${statusMeta[selected.status].className}`}><i/>{statusMeta[selected.status].label}</span><h2>{selected.name}</h2><p className="modal-area">{selected.area}</p><div className="detail-progress"><span><b>{selected.progress}%</b> readiness</span><span className="progress-track"><span className={`progress-fill ${selected.accent}`} style={{width:`${selected.progress}%`}}/></span></div><div className="detail-block"><small>NEXT MOVE</small><p>{selected.next}</p></div><div className="detail-block warning"><small>GATE / BLOCKER</small><p>{selected.blocker}</p></div><div className="detail-block"><small>VERIFICATION</small><p>{selected.verification}</p></div></article></div>}
  </main>;
}
